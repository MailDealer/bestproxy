Port 8888
Listen 0.0.0.0
Timeout 600
Allow 0.0.0.0/0
# Require basic auth — bestproxy sends Proxy-Authorization from forward.username/password.
BasicAuth e2euser e2epass
# Permit CONNECT tunnels to the origin's TLS port.
ConnectPort 443
